<local:OnboardingViewBase
    x:Class="Stopwatch.Views.OnboardingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Stopwatch.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <FlipView x:Name="Wizard" Margin="40">
            <FlipViewItem>
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image MaxWidth="320" Source="ms-appx:///Assets/Icons/icon_foreground.png" />
                    <TextBlock
                        Grid.Row="1"
                        HorizontalAlignment="Center"
                        Style="{StaticResource HeaderTextBlockStyle}"
                        Text="Stopwatch" />
                    <Button Grid.Row="2" HorizontalAlignment="Center">Get started</Button>
                </Grid>
            </FlipViewItem>
            <FlipViewItem>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Button
                        Grid.Row="2"
                        HorizontalAlignment="Center"
                        Command="{x:Bind ViewModel.GetStartedCommand}">
                        Let's go!
                    </Button>
                </Grid>
            </FlipViewItem>
        </FlipView>

        <PipsPager
            x:Name="FlipViewPipsPager"
            Margin="10"
            HorizontalAlignment="Center"
            VerticalAlignment="Bottom"
            NumberOfPages="{x:Bind Wizard.Items.Count}"
            SelectedPageIndex="{x:Bind Wizard.SelectedIndex, Mode=TwoWay}" />
    </Grid>
</local:OnboardingViewBase>
