<local:GetProViewBase
    x:Class="Stopwatch.Views.GetProView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Stopwatch.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:ex="using:Stopwatch.Extensions.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Grid>
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup x:Name="WindowStates">
				<VisualState x:Name="WideState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="720" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="HeaderGrid.Margin" Value="36,60,36,36" />
						<Setter Target="ContentGrid.Margin" Value="36,0,36,8" />
					</VisualState.Setters>
				</VisualState>
				<VisualState x:Name="NarrowState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="0" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="HeaderGrid.Margin" Value="16,60,16,36" />
						<Setter Target="ContentGrid.Margin" Value="16,0,16,8" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid x:Name="HeaderGrid" Margin="36,60,36,36" ColumnSpacing="8">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<Button Command="{x:Bind ViewModel.GoBackCommand}" Style="{ThemeResource NavigationBackButtonStyle}" />
			<TextBlock
				Grid.Column="1"
				Style="{ThemeResource TitleTextBlockStyle}"
				Text="{ex:Localize Key=FluentStopwatchPro}" />
		</Grid>
		<ScrollViewer VerticalAlignment="Center" Grid.Row="1" Padding="8">
			<Grid RowSpacing="8">
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<Grid x:Name="ContentGrid" MaxWidth="800" RowSpacing="8" ColumnSpacing="8" Grid.Row="1" Margin="36,0,36,8">
					<Grid.Resources>
						<Style x:Key="HeaderStyle" TargetType="TextBlock">
							<Setter Property="FontWeight" Value="Bold" />
							<Setter Property="FontSize" Value="20" />
							<Setter Property="HorizontalAlignment" Value="Center" />
							<Setter Property="Margin" Value="8" />
						</Style>
						<Style x:Key="CaptionStyle" TargetType="TextBlock">
							<Setter Property="FontSize" Value="14" />
							<Setter Property="HorizontalAlignment" Value="Center" />
							<Setter Property="TextAlignment" Value="Center" />
							<Setter Property="TextWrapping" Value="WrapWholeWords" />
						</Style>
						<Style TargetType="Image">
							<Setter Property="MaxWidth" Value="180" />
						</Style>
					</Grid.Resources>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>

					<StackPanel>
						<Image Source="ms-appx:///Assets/Pro/tab.png" />
						<TextBlock TextTrimming="CharacterEllipsis" Grid.Row="1" Style="{StaticResource HeaderStyle}" Text="{ex:Localize Key=MultipleStopwatches}" />
						<TextBlock Grid.Row="1" Style="{StaticResource CaptionStyle}" Text="{ex:Localize Key=MultipleStopwatchesDescription}" />
					</StackPanel>

					<StackPanel Grid.Column="1">
						<Image Source="ms-appx:///Assets/Pro/removeads.png" />
						<TextBlock TextTrimming="CharacterEllipsis" Grid.Row="1" Style="{StaticResource HeaderStyle}" Text="{ex:Localize Key=NoAds}" />
						<TextBlock Grid.Row="1" Style="{StaticResource CaptionStyle}" Text="{ex:Localize Key=NoAdsDescription}" />
					</StackPanel>

					<StackPanel Grid.Column="2">
						<Image Source="ms-appx:///Assets/Pro/customize.png" />
						<TextBlock TextTrimming="CharacterEllipsis" Grid.Row="1" Style="{StaticResource HeaderStyle}" Text="{ex:Localize Key=EndlessCustomization}" />
						<TextBlock Grid.Row="1" Style="{StaticResource CaptionStyle}" Text="{ex:Localize Key=EndlessCustomizationDescription}" />
					</StackPanel>

					<TextBlock TextAlignment="Center" Margin="8,24,8,0" Grid.Row="1" TextWrapping="WrapWholeWords" Text="{ex:Localize Key=FutureProFeatures}" FontSize="24" HorizontalAlignment="Center" Grid.ColumnSpan="3" />
				</Grid>
				<StackPanel Grid.Row="2" Spacing="4" Visibility="{x:Bind ViewModel.CurrentPrice, Mode=OneWay, Converter={StaticResource NonNullToVisibilityConverter}}">
					<TextBlock FontSize="20" FontWeight="Bold" Text="{ex:Localize Key=InAppPriceOnly}" HorizontalAlignment="Center" />
					<TextBlock FontSize="30" Text="{x:Bind ViewModel.CurrentPrice, Mode=OneWay}" HorizontalAlignment="Center" />
				</StackPanel>
			</Grid>
		</ScrollViewer>
		<StackPanel Grid.Row="2" Spacing="16" Margin="0">
			<Button FontSize="30" CornerRadius="0" Padding="30" HorizontalAlignment="Stretch" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.GetProCommand}">
				<StackPanel Orientation="Horizontal" Spacing="8">
					<FontIcon Glyph="&#xE7BF;" FontSize="30" />
					<TextBlock Text="{ex:Localize Key=GetProNow}" />
				</StackPanel>
			</Button>
		</StackPanel>
	</Grid>
</local:GetProViewBase>
