<local:GetProViewBase
    x:Class="Stopwatch.Views.GetProView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="using:Stopwatch.Extensions.Xaml"
    xmlns:local="using:Stopwatch.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid
            x:Name="ContentRoot"
            Margin="12,48,12,12"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Command="{x:Bind ViewModel.GoBackCommand}" Style="{ThemeResource NavigationBackButtonStyle}" />
            <TextBlock
                Grid.Column="1"
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="{ex:Localize Key=FluentStopwatchPro}" />
        </Grid>
        <ScrollViewer
            Grid.Row="1"
            Padding="8"
            VerticalAlignment="Center">
            <Grid RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid
                    Grid.Row="1"
                    MaxWidth="800"
                    Margin="36,0,36,8"
                    ColumnSpacing="8"
                    RowSpacing="8">
                    <Grid.Resources>
                        <Style x:Key="HeaderStyle" TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="FontSize" Value="20" />
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="Margin" Value="8" />
                        </Style>
                        <Style x:Key="CaptionStyle" TargetType="TextBlock">
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="TextAlignment" Value="Center" />
                            <Setter Property="TextWrapping" Value="WrapWholeWords" />
                        </Style>
                        <Style TargetType="Image">
                            <Setter Property="MaxWidth" Value="180" />
                        </Style>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel>
                        <Image Source="ms-appx:///Assets/Pro/tab.png" />
                        <TextBlock
                            Grid.Row="1"
                            Style="{StaticResource HeaderStyle}"
                            Text="{ex:Localize Key=MultipleStopwatches}"
                            TextTrimming="CharacterEllipsis" />
                        <TextBlock
                            Grid.Row="1"
                            Style="{StaticResource CaptionStyle}"
                            Text="{ex:Localize Key=MultipleStopwatchesDescription}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <Image Source="ms-appx:///Assets/Pro/removeads.png" />
                        <TextBlock
                            Grid.Row="1"
                            Style="{StaticResource HeaderStyle}"
                            Text="{ex:Localize Key=NoAds}"
                            TextTrimming="CharacterEllipsis" />
                        <TextBlock
                            Grid.Row="1"
                            Style="{StaticResource CaptionStyle}"
                            Text="{ex:Localize Key=NoAdsDescription}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <Image Source="ms-appx:///Assets/Pro/customize.png" />
                        <TextBlock
                            Grid.Row="1"
                            Style="{StaticResource HeaderStyle}"
                            Text="{ex:Localize Key=EndlessCustomization}"
                            TextTrimming="CharacterEllipsis" />
                        <TextBlock
                            Grid.Row="1"
                            Style="{StaticResource CaptionStyle}"
                            Text="{ex:Localize Key=EndlessCustomizationDescription}" />
                    </StackPanel>

                    <TextBlock
                        Grid.Row="1"
                        Grid.ColumnSpan="3"
                        Margin="8,24,8,0"
                        HorizontalAlignment="Center"
                        FontSize="24"
                        Text="{ex:Localize Key=FutureProFeatures}"
                        TextAlignment="Center"
                        TextWrapping="WrapWholeWords" />
                </Grid>
                <StackPanel
                    Grid.Row="2"
                    Spacing="4"
                    Visibility="{x:Bind ViewModel.CurrentPrice, Mode=OneWay, Converter={StaticResource NonNullToVisibilityConverter}}">
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="20"
                        FontWeight="Bold"
                        Text="{ex:Localize Key=InAppPriceOnly}" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="30"
                        Text="{x:Bind ViewModel.CurrentPrice, Mode=OneWay}" />
                </StackPanel>
            </Grid>
        </ScrollViewer>
        <StackPanel
            Grid.Row="2"
            Margin="0"
            Spacing="16">
            <Button
                Padding="30"
                HorizontalAlignment="Stretch"
                Command="{x:Bind ViewModel.GetProCommand}"
                CornerRadius="0"
                FontSize="30"
                Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="30" Glyph="&#xE7BF;" />
                    <TextBlock Text="{ex:Localize Key=GetProNow}" />
                </StackPanel>
            </Button>
        </StackPanel>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentRoot.Margin" Value="12,48,12,12" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ContentRoot.Margin" Value="24,60,24,24" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</local:GetProViewBase>
