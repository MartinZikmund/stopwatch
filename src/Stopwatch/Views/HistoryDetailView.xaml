<local:HistoryDetailViewBase
    x:Class="Stopwatch.Views.HistoryDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ex="using:Stopwatch.Extensions.Xaml"
    xmlns:local="using:Stopwatch.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:Stopwatch.ViewModels"
    mc:Ignorable="d">

    <Grid RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Grid
            x:Name="HeaderPanel"
            Margin="12,48,12,8"
            ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Command="{x:Bind ViewModel.GoBackCommand}" Style="{ThemeResource NavigationBackButtonStyle}" />

            <!--  Icon  -->
            <Border
                Grid.Column="1"
                Width="36"
                Height="36"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                CornerRadius="18">
                <TextBlock
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="18"
                    Text="{x:Bind ViewModel.HistoryEntry.Icon, Mode=OneWay}" />
            </Border>

            <TextBlock
                Grid.Column="2"
                VerticalAlignment="Center"
                Style="{ThemeResource TitleTextBlockStyle}"
                Text="{x:Bind ViewModel.HistoryEntry.Name, Mode=OneWay}"
                TextTrimming="CharacterEllipsis" />

            <!--  Export button  -->
            <Button
                Grid.Column="3"
                Style="{ThemeResource SubtleButtonStyle}"
                ToolTipService.ToolTip="{ex:Localize Key=Export}">
                <FontIcon Glyph="&#xE72D;" />
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Command="{x:Bind ViewModel.ExportToJsonCommand}" Text="JSON" />
                        <MenuFlyoutItem Command="{x:Bind ViewModel.ExportToCsvCommand}" Text="CSV" />
                        <MenuFlyoutItem Command="{x:Bind ViewModel.ExportToXmlCommand}" Text="XML" />
                        <MenuFlyoutItem Command="{x:Bind ViewModel.ExportToExcelCommand}" Text="Excel" />
                    </MenuFlyout>
                </Button.Flyout>
            </Button>

            <!--  Delete button  -->
            <Button
                Grid.Column="4"
                Command="{x:Bind ViewModel.DeleteCommand}"
                Style="{ThemeResource SubtleButtonStyle}"
                ToolTipService.ToolTip="{ex:Localize Key=Delete}">
                <SymbolIcon Symbol="Delete" />
            </Button>
        </Grid>

        <!--  Content  -->
        <ScrollViewer Grid.Row="1">
            <StackPanel
                x:Name="ContentRoot"
                MaxWidth="800"
                Margin="12,0,12,12"
                Spacing="24">

                <!--  Summary Card  -->
                <Grid
                    Padding="16"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Spacing="8">
                        <!--  Large time display  -->
                        <TextBlock
                            FontSize="36"
                            FontWeight="SemiBold"
                            Text="{x:Bind ViewModel.HistoryEntry.PausedElapsedTime, Mode=OneWay}" />

                        <!--  Meta info  -->
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <TextBlock Opacity="0.7" Style="{ThemeResource BodyTextBlockStyle}">
                                <Run Text="{ex:Localize Key=Started}" />
                                <Run Text=": " />
                                <Run FontWeight="SemiBold" Text="{x:Bind ViewModel.HistoryEntry.InitialStartTime, Mode=OneWay}" />
                            </TextBlock>
                            <TextBlock Opacity="0.7" Style="{ThemeResource BodyTextBlockStyle}">
                                <Run Text="{ex:Localize Key=Laps}" />
                                <Run Text=": " />
                                <Run FontWeight="SemiBold" Text="{x:Bind ViewModel.HistoryEntry.LapCount, Mode=OneWay}" />
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>

                    <!--  Restore button  -->
                    <Button
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        Command="{x:Bind ViewModel.RestoreCommand}"
                        Style="{ThemeResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE777;" />
                            <TextBlock Text="{ex:Localize Key=RestoreStopwatch}" />
                        </StackPanel>
                    </Button>
                </Grid>

                <!--  Laps Section  -->
                <StackPanel Spacing="8">
                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}" Text="{ex:Localize Key=Laps}" />

                    <!--  Laps Table  -->
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        CornerRadius="8">
                        <StackPanel>
                            <!--  Laps Header  -->
                            <Grid
                                Padding="12,8"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1"
                                ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="2*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    FontWeight="SemiBold"
                                    Opacity="0.7"
                                    Text="#" />
                                <TextBlock
                                    Grid.Column="1"
                                    FontWeight="SemiBold"
                                    Opacity="0.7"
                                    Text="{ex:Localize Key=LapTime}" />
                                <TextBlock
                                    Grid.Column="2"
                                    FontWeight="SemiBold"
                                    Opacity="0.7"
                                    Text="{ex:Localize Key=TotalTime}" />
                                <TextBlock
                                    Grid.Column="3"
                                    FontWeight="SemiBold"
                                    Opacity="0.7"
                                    Text="{ex:Localize Key=Note}" />
                            </Grid>

                            <!--  Laps List  -->
                            <ListView
                                ItemsSource="{x:Bind ViewModel.LapViewModels, Mode=OneWay}"
                                SelectionMode="None">
                                <ListView.ItemContainerStyle>
                                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="MinHeight" Value="40" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="vm:HistoryLapViewModel">
                                        <Grid Padding="12,8" ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="2*" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Opacity="0.6"
                                                Text="{x:Bind LapNumber}" />
                                            <TextBlock
                                                Grid.Column="1"
                                                FontWeight="SemiBold"
                                                Text="{x:Bind LapTime}" />
                                            <TextBlock
                                                Grid.Column="2"
                                                Opacity="0.8"
                                                Text="{x:Bind TotalTime}" />
                                            <TextBlock
                                                Grid.Column="3"
                                                Opacity="0.7"
                                                Text="{x:Bind Note}"
                                                TextTrimming="CharacterEllipsis" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="Narrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderPanel.Margin" Value="12,48,12,8" />
                        <Setter Target="ContentRoot.Margin" Value="12,0,12,12" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Wide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderPanel.Margin" Value="24,60,24,24" />
                        <Setter Target="ContentRoot.Margin" Value="24,0,24,24" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>

</local:HistoryDetailViewBase>
